name: Releaser

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  npm:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      id-token: write

    steps:
      - name: Checkout
        uses: actions/checkout@v6.0.2
        with:
          fetch-depth: 0

      - name: Setup Bun
        uses: oven-sh/setup-bun@v2.1.3

      - name: Setup Pantry
        uses: home-lang/pantry/packages/action@v0.8.16

      - name: Install Dependencies
        run: bun install

      - name: Publish
        run: pantry publish --npm --access public
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}

      - name: Create GitHub Release
        uses: stacksjs/action-releaser@v1.2.9
        with:
          files: |
            packages/ts-syntax-highlighter/bin/ts-syntax-highlighter-linux-x64.zip
            packages/ts-syntax-highlighter/bin/ts-syntax-highlighter-linux-arm64.zip
            packages/ts-syntax-highlighter/bin/ts-syntax-highlighter-windows-x64.zip
            packages/ts-syntax-highlighter/bin/ts-syntax-highlighter-darwin-x64.zip
            packages/ts-syntax-highlighter/bin/ts-syntax-highlighter-darwin-arm64.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
